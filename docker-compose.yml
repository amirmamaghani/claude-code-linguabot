version: "3.8"
services:
  linguabot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - claude-auth:/root/.claude
      - whisper-cache:/root/.cache
    tmpfs:
      - /tmp:size=512M

volumes:
  claude-auth:
  whisper-cache:
