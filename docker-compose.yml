services:
  bot-amir:
    build: .
    restart: unless-stopped
    env_file: .env.bot-amir
    volumes:
      - /opt/linguabot-claude/bot-amir:/root/.claude
      - /opt/linguabot-db/bot-amir:/app/db
    tmpfs:
      - /tmp:size=512M

  bot-ru:
    build: .
    restart: unless-stopped
    env_file: .env.bot-ru
    volumes:
      - /opt/linguabot-claude/bot-ru:/root/.claude
      - /opt/linguabot-db/bot-ru:/app/db
    tmpfs:
      - /tmp:size=512M
