version: "3.8"
services:
  linguabot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ~/.claude:/root/.claude:ro
      - ~/.claude.json:/root/.claude.json:ro
      - whisper-cache:/root/.cache
    tmpfs:
      - /tmp:size=512M

volumes:
  whisper-cache:
