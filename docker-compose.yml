services:
  bot-amir:
    build: .
    restart: unless-stopped
    env_file: .env.bot-amir
    tmpfs:
      - /tmp:size=512M

  bot-ru:
    build: .
    restart: unless-stopped
    env_file: .env.bot-ru
    tmpfs:
      - /tmp:size=512M
